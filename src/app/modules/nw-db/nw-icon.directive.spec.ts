import { NwIconDirective } from './nw-icon.directive';

describe('NwIconDirective', () => {
  it('should create an instance', () => {
    const directive = new NwIconDirective();
    expect(directive).toBeTruthy();
  });
});
